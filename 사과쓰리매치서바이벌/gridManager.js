const GRID_WIDTH = 8;
const GRID_HEIGHT = 8;

const SIZE = 7;
const objArr =
    Array.from({ length: SIZE }, () =>
        Array.from({ length: SIZE }, () => ({}))
    );

function GridManager() {

}